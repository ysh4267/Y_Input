# rune-frames-LLUL-coolglow-20260805 — 한색 광류 병합 · 융합 첫 오답 사건

**정답: ← ← ↑ ← (전부 정지)** · 2026-08-05 09:20 실전, 매크로가 **R D L D 오답 입력**(룬 소실).
증거 원본: `logs\rune-fail-20260805-092006-manual` (오답은 퍼즐이 닫혀 자동 스냅샷이 없어 수동 보존).

## 함정 (4각도 검증으로 확정)

1. **한색 광류 병합** — 퍼즐 오버레이의 파랑·청록 발광이 정적(교집합 생존)·고채도(sat80+)·
   발동 전과 다름(차분 통과)이라 어떤 픽셀 술어로도 안 걸러지고, requireWarm=false 마스크에서
   화살표 4개를 한 블롭(562×201px, a40958)으로 병합 → 박스 게이트(≤70px)에서 후보 전멸 →
   ④가 2.4초 내내 실패. 웜톤 게이트만 켜면 같은 프레임에서 4개 블롭(22~25px, a310~406)이
   (505,245)(592,240)(654,243)(780,236)으로 깨끗이 분리된다.
2. **⑦ 잡줄** — 좌측 UI 잔재(x 164~613, 필 중심 0.16W 이탈)로 3+외삽 구성.
3. **융합 부분 잡줄(사건 당시)** — 중심 게이트가 없어 필 우측 0.26W 이탈 잡신호(지형 돔
   림라이트·필 테두리 원호·잎사귀 술·"일반물자 지원" UI 보드)를 채택, 정지 잡음이라
   시그니처 런·AxisStable 전부 통과 → R D L D 오답 4/4.

## 기대 (수정 후 불변식)

- ④ 실패 유지(웜톤을 ④ 정식 단계로 넣는 안은 기각 — DLUU에서 웜 마스크가 ↑을 →로
  오분류한 줄을 ④가 채택해버림. 웜톤은 **위치 전용** 폴백이어야 한다)
- ⑦ 실패(중심 게이트가 좌측 잡줄 차단)
- **⑦w 웜톤 줄(위치만) = 505 592 654 780** — 실전은 이 위치를 채택하고 방향은 로컬 관찰이
  ← ← ↑ ←로 판정한다
- ⑧ 소스 융합도 웜 후보 편입 + 중심 게이트로 같은 진짜 줄 채택(이중 방어)
- ② 발동 전 차분 = ← ← ↑ ← (웜 마스크 진단 경로 — 정답 대조 기준)
